<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TechnoService.Views.AuthorizationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TechnoService.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Width="500" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8">
        <TextBlock Text="Вход" HorizontalAlignment="Center" Style="{ThemeResource TitleTextBlockStyle}"/>
        <TextBox PlaceholderText="Логин" TextChanging="OnTextChanging" MaxLength="50"/>
        <PasswordBox PlaceholderText="Пароль" PasswordChanging="OnPasswordChanging" MaxLength="50"/>
        <Button Content="Войти"
                Click="OnLoginButtonClick"
                Style="{ThemeResource AccentButtonStyle}" 
                HorizontalAlignment="Stretch"/>
    </StackPanel>
</Page>
