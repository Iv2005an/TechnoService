<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TechnoService.Views.StaffPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TechnoService.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <CommandBar>
            <AppBarButton Icon="Add" Label="Добавить"/>
            <CommandBar.Content>
                <TextBlock Text="Персонал" Margin="12,14"/>
            </CommandBar.Content>
        </CommandBar>
        <controls:DataGrid ItemsSource="{x:Bind Staff}" AutoGenerateColumns="False">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                <controls:DataGridTextColumn Header="Имя" Binding="{Binding Name}"/>
                <controls:DataGridTextColumn Header="Тип" Binding="{Binding Type}"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </StackPanel>
</Page>